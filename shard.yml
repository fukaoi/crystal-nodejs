name: nodejs
version: 0.1.0

authors:
  - fukaoi 
crystal: 0.27.2
license: MIT

targets:
  nodejs:
    main: src/nodejs.cr  
  lucky:
    main: lib/lucky_cli/src/lucky.cr

scripts:
  postinstall: cd lib/nodejs;g++ ext/test.c -o bin/libnode_main -L./ ./ext/node_main.o ./ext/libnode.so.64 -Wl,-rpath=./ext

dependencies:
  lucky_cli:
    github: luckyframework/lucky_cli

development_dependencies:
  ameba:
    github: veelenga/ameba
    version: ~> 0.9.0

